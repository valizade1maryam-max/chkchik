<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The ONLY runtime permission the app needs for its core feature. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Needed only for saving images into Pictures/ on Android 9 (API 28) and below. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!--
        Privacy: the core camera/pose/overlay feature works fully offline and needs no
        network access. INTERNET is only used by the Explore / Inspiration screen (Stage
        16) to fetch photos from an official third-party API (see ExploreRepository) -
        nothing else in the app makes a network call.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" tools:node="remove" />

    <!--
        Camera is not hard-required at install time so that a device without a usable
        camera can still open the app and get a clear message instead of "can't install".
    -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!--
        Stage 28: package visibility (Android 11+) for the Settings > Contact Me action - lets
        the app target the Telegram app's package explicitly (see util/ContactUtils.kt) and,
        should that fail, hand the https://t.me/... link to whatever browser is installed.
    -->
    <queries>
        <package android:name="org.telegram.messenger" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- No data worth backing up (no accounts, no database); nothing leaves the device. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChikChik">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.ChikChik">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
